node_modules
dist
.git
.gitignore
.DS_Store
*.log
.idea
.vscode
coverage
# Large content trees that aren't needed for the build.
# The runtime issues/ tree is supplied via bind-mount (see
# unraid/cinefex-online.xml); covers/ and fonts/ are explicitly
# COPY'd from the builder, so excluding the dev-only working trees
# here keeps the build context small.
issues/
.docker-issues/

<?xml version="1.0" encoding="UTF-8"?>
<Container version="2">
  <Name>cinefex-online</Name>
  <Repository>ghcr.io/djcallyman/cinefex-online:latest</Repository>
  <Registry>ghcr.io</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/DJCallyman/Cinefex-Online/issues</Support>
  <Project>https://github.com/DJCallyman/Cinefex-Online</Project>
  <Overview>The complete Cinefex magazine archive (1980-present), served as a static web app.
Image rebuilds on every merge to main. Issues content is supplied at runtime via a bind mount
on your share.</Overview>
  <Category>Books:Comics</Category>
  <WebUI>http://[IP]:[PORT:8586]/</WebUI>
  <TemplateURL/>
  <Icon>https://raw.githubusercontent.com/DJCallyman/Cinefex-Online/main/public/favicon.ico</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled>0</DateInstalled>
  <DonateText/>
  <DonateLink/>
  <Description>The web archive. Issues are read from your share; covers and fonts are baked into the image.
Update the image by re-running the container (unraid pulls :latest on restart when configured).</Description>
  <Environment>
    <Variable>
      <Key>ISSUES_DIR</Key>
      <Value>/issues</Value>
      <Mode/>
      <Description>Container path the issues/ share is bind-mounted to. Don't change unless you also update the Volume mapping.</Description>
    </Variable>
    <Variable>
      <Key>TZ</Key>
      <Value>Australia/Sydney</Value>
      <Mode/>
      <Description>Container timezone for log timestamps. Set to whatever you like.</Description>
    </Variable>
  </Environment>
  <Labels/>
  <Config Name="Web Port" Target="8586" Default="8586" Mode="tcp" Description="Host port mapped to container port 80 (where nginx listens). Must match the upstream port your reverse proxy forwards to." Type="Port" Display="always" Required="true" Mask="false">8586</Config>
  <Config Name="Issues Share" Target="/issues" Default="/mnt/user/issues" Mode="rw" Description="Path on the unraid host to your issues/ tree. Each issue should be a numbered subdirectory (1, 2, ..., N) with manifest.xml and *.html files." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/issues</Config>
  <Config Name="Image Tag" Target="cinefex-online" Default="latest" Mode="" Description="Which tag to pull. :latest is rebuilt on every merge to main." Type="Variable" Display="advanced" Required="false" Mask="false">latest</Config>
  <Port>8586:80</Port>
  <Data>
    <Volume>
      <HostDir>/mnt/user/issues</HostDir>
      <ContainerDir>/issues</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
  </Data>
  <PluginURL/>
</Container>
